#include <Arduino.h>
/*
int out = 0;
byte in = 0;
byte pinOut = 10;
int valor = 0;
int time = 0;
int time1 = 0;
float diferencial;

void setup()
{
    // inicializar puerto serie
    Serial.begin(9600);
}

void loop()
{
    // leer del pin A0 como
    time1 = millis();
    out = analogRead(A0);
    diferencial = out - valor;
    // Realizar derivada:
    diferencial /= (time1 - time);
    // Imprimir derivada:
    Serial.println(diferencial);
    // Retomar valores
    time = millis();
    valor = analogRead(A0);
    delay(1);
}
*/